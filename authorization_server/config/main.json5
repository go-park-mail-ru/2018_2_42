// Все конфигурационные записи сервера.
{
  // конфигурация, которая используется при открытии соединения к postgres в db.Open().
  // host 'database' == имя контейнера с базой в docker network.
  "data_source_name": "postgres://postgres:@database:5432/postgres?sslmode=disable",
}
